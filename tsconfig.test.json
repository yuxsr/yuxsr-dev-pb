{
  /* 型レベルテスト専用の設定。何も出力せず検査だけを行う。
     test/types.ts は利用側と同じ自己参照 import (from 'yuxsr-dev-pb') を使うため、
     package.json の "exports" と生成された index.d.ts を経由して解決される。
     つまり「公開面が利用側から正しく見えるか」の検証になる。 */
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "files": [],
  "include": ["test/**/*.ts"]
}
